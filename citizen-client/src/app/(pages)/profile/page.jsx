import Profile from "@/components/Pages/Profile/Profile";

export const metadata = {
  title: "Citzen | Profile",
  description: "Citezen Profile",
};

const ProfilePage = () => {
  return (
    <div className="custom-container">
      <Profile />
    </div>
  );
};

export default ProfilePage;
